# Never commit secrets to a public repo — git history is permanent.
.env
.env.*
*.key
*.pem
secrets.*
credentials.*

# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
.idea/
.vscode/

# Large unoptimised source art. Export web-sized versions into assets/ instead.
*-original.*
/source-art/
